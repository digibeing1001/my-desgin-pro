---
name: my-desgin
description: >
  专业平面设计与VI视觉识别系统生成技能。支持多家AI生图模型（Seedream/Nano Banana/混元/GPT Image/Imagen 4/Flux 2/Ideogram/Stable Image），
  通过需求追问→竞品分析→设计哲学创建→Moodboard+样稿生成→VI物料扩展→合规审查+审美自检→落地指导的6阶段完整工作流，
  产出可用的平面设计作品。融合canvas-design设计哲学方法论驱动视觉创作。
  支持矢量图(AI/EPS/SVG)和PSD格式输出，内置国内设计合规规范自动审查
  （商标法、广告法、国家通用语言文字法、著作权法、字体版权、招牌规范、食品标签、商场规范、3C认证、包装刀图等）。
  覆盖14大行业合规：医疗/酒类/烟草/教育/房产/食品/金融/化妆品/互联网/零售/母婴/汽车/服装/文旅。
  触发场景（只要命中任一条即触发，无需全部匹配）：
  【Console启动】design gui、打开console、启动gui、gdpro、localhost:3005、设计控制台、设计台
  【品牌类】(1)品牌设计/品牌视觉/品牌升级/品牌重塑/品牌焕新/企业形象/CI/VI/品牌规范/品牌手册/品牌色/品牌字体
  (2)logo设计/标志设计/商标设计/图标设计/Icon设计/动态标识
  【物料类】(3)海报/名片/招牌/易拉宝/宣传册/折页/画册/传单/DM单/菜单设计
  (4)包装设计/包装刀图/食品标签/化妆品包装/礼盒设计
  (5)户外广告/门头/店面招牌/灯箱/展架/展会展台/展览设计
  【电商数字类】(6)电商主图/详情页/Banner/店铺装修/社交媒体图/公众号配图/小红书图/短视频封面
  【VI系统类】(7)辅助图形/色彩系统/字体系统/排版规范/设计系统/Design System/品牌规范手册
  (8)品牌物料批量生成/VI物料扩展/衍生品设计/周边设计
  【审查输出类】(9)AI生图+设计合规审查/去AI标识/矢量图输出/SVG/PSD输出
  (10)竞品视觉分析/Moodboard制作/配色方案/色彩方案/设计哲学表达
  【泛设计意图】(11)帮我设计/做个图/出个图/画个/设计一下/搞个设计/做个XX图/视觉创作/视觉设计
  (12)任何需要视觉产出的请求（当用户要产出图片、视觉物料、品牌元素、设计文件时）
---

# Graphic Design Pro — 专业平面设计 Skill

基于多模型AI生图（Seedream/Nano Banana/混元/GPT Image/Imagen 4/Flux 2/Ideogram/Stable Image） + canvas-design设计哲学方法论 + brand-cog品牌战略方法论 + design-critique设计评审 + svg-draw矢量绘制 + 国内合规审查的专业平面设计工作流。

## 🚀 自动触发规则（R-TRIGGER）

> **只要意识到用户的意图涉及品牌/视觉/设计产出，就必须立即加载本Skill。宁可误触不可漏触。**

| # | 规则 | 违反后果 |
|---|------|---------|
| R-T1 | **意图识别触发**：用户表达任何"做设计/出图/画/视觉/品牌"意图时，立即 `use_skill my-desgin`，不用等用户说出精确术语 | 漏触发 = 用户得不到专业流程 |
| R-T2 | **模糊意图也触发**：用户说"帮我搞个XX""做个XX的图""设计一下XX"——即使措辞模糊，只要涉及视觉产出，即触发 | 漏触发 = 丧失专业性 |
| R-T3 | **子任务也触发**：如果用户的大任务中包含任何设计子环节（如"做个产品页面"→包含Banner设计），设计环节必须用本Skill | 跳过 = 设计降质为非专业输出 |
| R-T4 | **合规审查独立触发**：用户只要求"审查这个设计/看看合不合规"时，也触发本Skill（进入Phase 5） | 不触发 = 合规审查缺失 |
| R-T5 | **不与纯代码UI冲突**：用户要求"写一个网页/App界面"（纯代码实现）→ 用 ui-design-review / frontend-design；但若涉及品牌视觉产出（Logo/品牌色/VI规范），必须同时触发本Skill | 只用UI Skill = 品牌一致性缺失 |
| **R-T6** | **使用方式偏好询问（R-UI-CHOICE）**：本 Skill 被 `use_skill` 加载时，**禁止自动启动 Console**。**必须先询问用户**：「你想通过哪种方式使用？① **对话界面**（在这里直接文字对话，适合快速沟通和确认）② **图形界面/Console**（打开可视化前端，适合查看设计稿、Moodboard、合规报告等结构化输出）」。若用户选择**对话界面** → 继续在当前对话中执行 Skill 工作流；若用户选择**图形界面** → 执行 `python scripts/launch_console.py --status` 检测 → 未运行则后台启动 → 返回 URL。用户后续随时可输入 `design gui`/`打开console`/`启动gui` 切换为图形界面。**禁止在未经用户明确同意的情况下自动启动 Console** | 自动启动 = 用户被迫使用图形界面 = 打断原有对话习惯 |
| **R-T7** | **Console 按需启动**：当用户**明确输入** `design gui` / `打开console` / `启动gui` / `localhost:3005` / `gdpro` 时，检测 Console 是否已运行，未运行则启动，然后用 `preview_url` 打开。**禁止在用户未主动要求时检测或启动 Console** | 未经请求自动启动 = 侵入式行为 = 用户反感 |

**触发关键词速查（不限于这些，任何同类意图均触发）：**

| 类别 | 触发词 |
|------|--------|
| 🖥️ Console | `design gui`、`打开console`、`启动gui`、`gdpro`、`localhost:3005`、`设计控制台`、`设计台` |
| 🏷️ 品牌 | 品牌设计、VI、CI、品牌视觉、品牌升级、品牌重塑、企业形象、品牌手册、品牌规范 |
| 🎨 Logo | logo、标志、商标、icon、图标、动态标识、徽标 |
| 📦 物料 | 海报、名片、招牌、易拉宝、宣传册、折页、画册、传单、DM、菜单、Banner、主图、详情页 |
| 🎁 包装 | 包装、刀图、食品标签、礼盒、瓶贴、化妆品包装 |
| 🏪 户外 | 门头、店面、灯箱、展架、展台、展览、户外广告 |
| 🛍️ 电商 | 电商图、店铺装修、社交媒体图、公众号图、小红书图、封面图 |
| 🎯 VI系统 | 辅助图形、色彩系统、字体系统、排版规范、设计系统、品牌物料、衍生品、周边 |
| 🔍 审查 | 合规审查、设计审查、合规检查、字体版权、商标合规 |
| 💬 泛意图 | 帮我设计、做个图、出个图、画个、搞个设计、设计一下、视觉创作 |

---

## 🧠 思维框架（贯穿全流程）

> **不是每个都要用，按需选配 2-4 项。** 设计项目越复杂，用得越多。

| 框架 | 符号 | 什么时候用 | 在哪个Phase |
|------|------|-----------|------------|
| 第一性原理 | 🔬 | 用户需求模糊时，追到最根本的设计目的 | Phase 1 追问 |
| 奥卡姆剃刀 | ✂️ | 设计流程/物料列表太繁时，砍掉多余 | Phase 4 物料选择 |
| 逆向思维 | 🔄 | 执行前先想"这个设计最可能怎么翻车" | 每个 Phase 开始前 |
| 反脆弱理论 | 💪 | 出了错不光修好，还要把教训存成规则 | R-E 自进化 |
| 二八法则 | 📈 | 物料多但核心只有几个时，优先搞定关键的 | Phase 4 优先级 |
| 复利思维 | 🌱 | 设计"每用一次就比上次更好"的审美积累 | R-A 审美学习 |
| 贝叶斯思维 | 📊 | 用户每轮反馈后，调整对审美偏好的判断 | 全程 |
| **位置四问** | 📐 | 每个物料开工前确认叙事角色/观众距离/视觉温度/容量估算（融入自huashu-design） | Phase 3-4 每个物料前 |

---

## 🎯 模式路由（项目复杂度分级）

> **不在简单项目上做重型流程。** 根据项目规模选择合适的深度。

| 模式 | 何时用 | 做法 |
|------|--------|------|
| 🟢 **轻量级** | 单一物料（一张海报/一个Logo） | Phase 1 快速追问(3轮) → Phase 3 直接出图 → Phase 5 合规检查 |
| 🟡 **中等** | 小型品牌套件（Logo+名片+1-2物料） | Phase 1 → Phase 2(压缩) → Phase 3 → Phase 4(精选物料) → Phase 5 |
| 🔵 **深度** | 完整VI系统（多物料+多场景） | 完整6阶段，每步充分展开 |
| 🔴 **重构** | 已有品牌升级/重塑 | 深度模式 + 额外回溯：旧品牌资产盘点、迁移策略、变更影响 |
| 🟣 **产品渲染** | 产品效果图/电商主图/详情页/包装产品图/3D渲染 | Phase 1(产品访谈+参考图检查) → PIF锁定 → Phase 3(文生图首图) → 一致性链路(多角度) → Phase 5 |

**路由判断流程：**
```
收到设计需求
    ↓
涉及产品渲染/效果图/3D渲染/电商主图/包装产品图？
    ↓ 是          ↓ 否
产品渲染模式    是单一物料？
                 ↓ 是          ↓ 否
               轻量级        是全新品牌还是升级？
                                ↓ 全新          ↓ 升级
                             物料<5个？       重构模式
                             ↓ 是    ↓ 否
                           中等模式  深度模式
```

---

## ⚠️ Top 5 翻车场景（每Phase开始前快速扫描）

| # | 翻车模式 | 表现 | 怎么防 |
|---|---------|------|--------|
| 1 | **品牌名漂移** | 不同产出用了不同品牌名 | R1逐字锁定 + brand-profile.md |
| 2 | **需求理解偏差** | 做完了才发现不是用户想要的 | Phase 1 Clarify追问法（见下）+ R8逐步确认 |
| 3 | **风格分裂** | 不同物料看起来不像同一个品牌 | R3-R7锁定 + 审美档案 + 辅助图形贯穿 |
| 4 | **合规后置** | 做完才发现违法 | R9合规前置 + Phase 5双重审查 |
| 5 | **过度设计** | 物料太多、流程太长 | 奥卡姆剃刀 + 模式路由 + 二八法则 |

---

## 核心工作流

```
Phase 1: 需求追问(Clarify) → Phase 2: 竞品分析+设计哲学创建 → Phase 3: Moodboard+样稿生成 → Phase 4: VI物料扩展 → Phase 5: 合规审查+审美自检 → Phase 6: 落地指导
```

---

## ⛓️ 品牌一致性铁律（全流程强制执行）

> **这不是建议，是硬规则。违反 = 产出作废。**

品牌名确认后，**立即**在项目目录下创建 `brand-profile.md`，写入锁定项。

### 硬规则

| # | 规则 | 违反后果 |
|---|------|---------|
| R1 | **品牌名逐字锁定**：后续所有产出必须逐字引用 brand-profile.md，不得擅自替换、缩写、意译或混用 | 产出作废，重做 |
| R2 | **禁止自作主张改名**：不得将用户确认的品牌名"优化"为自认为更好的名字 | 严重违规 |
| R3 | **色彩值锁定**：所有物料必须使用 brand-profile.md 中的 HEX 值 | 产出作废 |
| **R3-C** | **Console 设计师档案优先**：如果 `.gdpro/designer-profile.json` 存在，其审美偏好覆盖默认风格建议 | 风格漂移 |
| R4 | **字体锁定**：标题/正文字体确定后，所有物料统一使用 | 产出作废 |
| R5 | **设计哲学锁定**：Phase 2 的设计哲学是全流程创作纲领，Phase 3-6 不得偏离 | 产出作废 |
| R6 | **每阶段开头读 brand-profile.md**：进入新 Phase 时必须先读取确认 | 跳过 = 违规 |
| R7 | **品牌口号锁定**：不得在不同物料中使用不同口号 | 产出作废 |
| R8 | **逐步确认，禁止跑通流程**：每个Phase/每个核心物料完成后，必须暂停等待用户确认 | 跳过确认 = 产出无效 |
| R9 | **合规审查前置**：出品每个物料前，先扫描 `references/design-compliance.md` 内化到prompt | 做完再查 = 违规产出 |

---

## 🖥️ Console 前端协议（R-CONSOLE）

> **Graphic Design Pro Console 是本 Skill 的远程可视化终端，不是独立应用。**
>
> 违反以下规则 = Console 与 Skill 能力割裂 = 用户获得不完整/错误的设计产出。

### 架构铁律

| # | 规则 | 违反后果 |
|---|------|---------|
| R-CONSOLE-1 | **Console 不产生设计回复**：所有设计回复必须由 Skill 生成并通过 Gateway 返回。Console 禁止本地 mock、模拟或兜底回复。 | 用户收到假数据，信任崩塌 |
| R-CONSOLE-2 | **Console 不独立执行工作流**：Phase 推进、设计哲学创建、合规审查、设计评审等全部业务逻辑由 Skill 执行。Console 只负责渲染 Skill 的输出。 | 工作流断裂，质量门禁失效 |
| R-CONSOLE-3 | **阶段推进权归 Skill**：Console 用户点击「推进」后，必须将请求转发给 Skill。Skill 检查完成度（R8）后返回确认/拒绝。Console 不得自行推进阶段。 | 阶段跳过，产出不完整 |
| R-CONSOLE-4 | **结构化输出渲染**：Skill 返回的 `structuredOutput`（合规报告/设计哲学/Moodboard/评审评分等），Console 必须渲染为对应的 UI 组件，不得只显示纯文本。 | 用户无法阅读结构化数据 |
| R-CONSOLE-5 | **Context 完整性**：Console 每次调用 Skill 时，必须传递完整的 context（设计师档案 + 知识库 + 项目资产 + 当前阶段）。禁止省略或简化。 | Skill 丢失上下文，产出漂移 |
| R-CONSOLE-6 | **双向数据同步**：Console 中的用户操作（采纳资产、修改档案、上传文件）必须实时同步到 `.gdpro/` 目录，确保 Skill 能读取最新状态。 | 数据不一致，决策基于过期信息 |
| R-CONSOLE-7 | **离线即阻塞**：未连接 Gateway/Skill 时，Console 必须显示阻塞状态，禁止以「演示模式」「离线模式」等名义提供假功能。 | 用户误以为功能可用 |
| **R-CONSOLE-8** | **自动 Gateway 连接**：从 Agent 工具启动 Console 时，`launch_console.py` 必须自动检测启动来源（环境变量 → 父进程链 → 配置探测 → 健康检查），读取对应 Agent 的 Gateway 地址与 Token，并在打开浏览器时自动注入 `?gateway=&token=&env=` URL 参数。Console 前端收到注入参数后必须立即自动发起连接，禁止要求用户手动填写 Gateway 地址。 | 用户多一步操作 = 体验断裂，尤其非技术用户 |

### Skill 端对 Console 的支持

| 能力 | Skill 输出格式 | Console 渲染组件 |
|------|---------------|-----------------|
| 合规审查报告 | `structuredOutput.type: "compliance_report"` | ComplianceReport.jsx |
| 设计哲学 | `structuredOutput.type: "design_philosophy"` | DesignPhilosophyCard.jsx |
| Moodboard | `structuredOutput.type: "moodboard"` | MoodboardGrid.jsx |
| 设计评审 | `structuredOutput.type: "critique"` | CritiqueScoreCard.jsx |
| 色彩系统 | `structuredOutput.type: "color_system"` | ColorPalette.jsx |
| VI 规范手册 | `structuredOutput.type: "vi_manual"` | VIManualViewer.jsx |
| 品牌档案 | `syncFiles[".gdpro/brand-profile.md"]` | BrandProfileEditor.jsx |

### 执行规则

1. Skill 启动时，检查请求来源。如果是 `source: "console"`，按 Console 协议处理
2. Skill 返回的每条消息，必须包含 `phase` 和 `phaseAction`（如有阶段变更）
3. Skill 执行完任何操作后，必须将更新的数据写回 `.gdpro/` 目录
4. Console 渲染 Skill 输出时，优先使用 `structuredOutput`，fallback 到 `text`
5. **用户输入 `design gui` / `gdpro` / `localhost:3005` 或明确要求打开图形界面时**（R-T6 已询问用户偏好且用户选择图形界面，或 R-T7 用户主动触发）：
   - 执行 `python scripts/launch_console.py --status` 检测 Console 是否已运行
   - 如果已运行：返回 URL `http://localhost:{port}`，可用 `preview_url` 直接打开
   - 如果未运行：`Start-Process python -ArgumentList "scripts/launch_console.py --port 3005 --silent"` 后台启动 → 等待 2 秒 → 用 `preview_url` 打开 `http://localhost:3005`
   - Console 服务的是 `console/dist/` 目录下的预构建静态文件
   - 如果 `console/dist/index.html` 不存在，提示用户先 `cd console && npm run build`
   - 启动成功后，告知用户 Console URL 和关闭方式（`python scripts/launch_console.py --stop`）
| R10 | **包装物料双产出规则**：实体包装必须同时提供①**结构刀图/展开图**（生产用）+②**成品效果图/Mockup**（展示用），两者缺一不可 | 只有刀图 = 缺少视觉确认；只有效果图 = 无法落地生产 |
| R11 | **素材预渲染复用规则（R-ASSET）**：Logo/品牌文字/辅助图形等跨物料重复使用的元素，必须在首次确认后**立即预渲染为高清PNG素材文件**保存到项目 `assets/` 目录；后续所有物料（包装/网站/海报/名片等）直接引用这些素材文件，不得重新绘制或让AI生图生成 | 每次重绘 = 结构漂移、比例偏差、颜色不一致；违反 = 产出作废 |
| **R12** | **核心资产协议（R-BRAND-ASSET）**：涉及具体品牌时，按「Logo > 产品图 > UI截图 > 色值 > 字体」的识别度优先级采集真实资产，写入 `brand-spec.md`，所有产出必须引用真实资产路径。禁止用CSS剪影/SVG手画/通用渐变代替真实品牌资产（融入自huashu-design） | 用通用素材代替 = 品牌识别度归零，产出的是「任何品牌都长一样的通用设计」 |
| **R15** | **用户提供素材强制使用原则（R-USER-ASSET）**：用户上传/提供的任何素材（Logo/产品图/参考图/品牌手册等）必须在接收后**立即保存到 `assets/user-uploads/` 并登记到 `brand-spec.md`**；后续所有产出（生图 prompt / HTML / Canvas / 合规审查）必须优先引用该素材。禁止忽略、遗忘或擅自用 AI 重绘替代用户提供的素材。**用户提供 Logo → 立即跳过 Logo 生成步骤** | 忽略用户素材 = 品牌识别度归零 + 用户信任崩塌；擅自重绘 = 违规 |
| **R16** | **产出路径分叉规则（R-PATHWAY）**：明确区分两种独立产出路径——① **精确排版稿**（HTML/Canvas/SVG/代码生成，像素级精确，可直接交付印刷/数字端）；② **AI 概念图**（生图模型基于 prompt 重新创作，用于风格探索/氛围参考）。**禁止将精确排版稿作为 prompt 让 AI 生图模型"重新生成"以图获得"相同但更好的图"——AI 生图不是渲染引擎，无法像素级复现 HTML 布局**。如果需要"效果图"，必须先确认用户要的是「精确设计稿」还是「场景 Mockup」 | 混淆两条路径 = HTML 设计稿被 AI 重新扭曲，产出与预期严重不符 |
| **R17** | **场景化设计真实锚定原则（R-SCENE）**：涉及前台/门头/展墙/办公室等真实空间设计时，必须基于用户提供的场景照片进行。**单张照片无法判断精确绝对尺寸**，必须通过「已知尺寸参照物」或「用户手动标注」锁定真实尺寸后，方可进入设计。效果图必须使用**图生图/图像融合**技术将设计稿融入真实场景，保持原场景的光照、透视、材质一致。**禁止脱离场景照片凭空设计"通用前台"后声称是"该位置的效果图"** | 脱离真实场景 = 比例错误 + 材质冲突 + 光照虚假，效果图无法落地 |
| **R18** | **AI 生图边界原则（R-AI-BOUNDARY）**：AI 生图模型是**创意生成工具，不是精确设计工具，更不是 3D 渲染引擎**。涉及**精确排版、跨图一致性、3D 结构折叠（包装）、严格品牌色复现**的场景，禁止使用 AI 生图作为最终交付路径。这些场景必须使用确定性工具：Canvas-design（精确排版）、svg-draw（矢量）、3D 软件/专业包装工具（结构渲染）。**每次使用 AI 生图前，必须先过 `references/ai-image-limitations.md` 自检清单**。禁止以"prompt 写得不够好"为由反复尝试突破物理不可能 | 技术路径错误导致空间扭曲、一致性崩溃、无法落地，返工成本远高于使用正确工具 |
| **R13** | **事实验证先于假设（R-FACT）**：涉及具体产品/公司/事件时，第一步必须验证存在性/发布状态/版本号，写入 `product-facts.md`，禁止凭训练语料做断言（融入自huashu-design） | 事实错了，设计方向再对也是歪的 |
| **R14** | **内容诚实测试（R-HONESTY）**：每个视觉元素加入前问自己「如果去掉它，信息是否有损？」装饰性元素不加，有内在关联的元素必须加（融入自huashu-design） | 填充内容 = 稀释品牌品味，传递「不专业」信号 |
| **R19** | **交付物源文件强制规则（R-SOURCE）**：所有设计交付物必须同时提供①**可编辑源文件**（Photoshop .PSD / Illustrator .AI / .EPS / Sketch .Sketch / Figma .fig / 保留完整图层的原生格式）和②**预览/印刷文件**（PNG/JPG/PDF）。**禁止仅以位图、AI生成图或HTML截图作为唯一交付物**。源文件必须满足：图层完整未合并、文字保持可编辑（未转曲/未栅格化）、矢量路径保留（未转位图）、颜色模式正确（印刷CMYK/数字RGB）。用户明确要求"源文件"时，不得用"预览图+说明"替代 | 无源文件 = 用户无法修改/无法印刷落地/无法二次延展；违反 = 交付作废 |
| **R20** | **HTML 效果图定位规则（R-HTML）**：HTML/CSS/Canvas 生成的渲染图是**精确排版参考稿**，用于快速确认设计方向、布局、字体和间距。**HTML 截图绝对不能直接用于印刷或作为最终交付物**，因为它本质是屏幕 RGB 渲染（72–96 dpi），缺少出血线、裁切线、套印标记、颜色条等印刷必需标记，且印刷厂不接受 HTML 截图。涉及印刷的物料，必须在 HTML 确认后，在 Illustrator/Photoshop/InDesign 中**重绘**并添加完整印刷标记，转换为 CMYK/300dpi，导出 PDF/X-1a。禁止向用户交付 HTML 截图并声称"可直接印刷" | 用户拿 HTML 截图去印刷 = 色差/锯齿/缺标记/被印刷厂拒收 = 严重交付事故 |
| **R21** | **印刷标示线强制规则（R-PRINT-MARKS）**：所有涉及实体印刷的交付物（名片/海报/画册/包装刀图/招牌等），预览文件和刀图上必须包含完整的**印刷标示系统**：①出血线（3mm）、②裁切标记（Crop marks）、③套印标记（Registration marks）、④颜色条（Color bars）、⑤折叠线/模切线（包装刀图）。缺失任何一项 = 印刷厂无法准确生产 = 交付作废 | 缺标示线 = 裁切偏差/套色不准/无法校色 = 印刷废品 |
| **R22** | **决策辅助强制规则（R-ADVISOR）**：当用户在材质/工艺/品类选择环节回答"不知道""你推荐""看情况""随便"时，**禁止跳过或直接替用户决定**。必须进入顾问模式：①读取 `references/material-guide.md` 对应品类 → ②提供 **2–3 个选项**，每个选项包含**适用理由 + 核心优点 + 局限 + 市场参考价格** → ③等用户明确选择后方可继续。**禁止不提供选项就直接动手** | 跳过决策 = 用户拿到不符合预期/预算的材质 = 返工或信任崩塌 |
| **R23** | **最终交付物本地归档规则（R-DELIVER）**：所有用户**最终确认、明确表示"采用/满意/就这个"**的产物，必须在 Phase 6 最终交付时归档到用户**桌面文件夹**。路径格式：`C:\Users\{用户名}\Desktop\{品牌名}_交付物_YYYYMMDD\`。文件夹结构：①`01_源文件/`（PSD/AI/EPS/Sketch/Figma 等可编辑文件）②`02_预览文件/`（PNG/JPG/PDF 等预览/印刷文件）③`03_规范文档/`（brand-profile.md、VI规范手册、合规报告等）。**归档前必须获得用户最终确认**："以上文件是否全部满意？确认采用后我将保存到桌面文件夹。"**禁止保存用户未确认、未明确表示采用的中间稿/草稿**。若用户无桌面路径权限，回退到项目目录 `deliverables/` | 无本地归档 = 用户找不到最终文件 = 交付作废 |
| **R23-A** | **归档即最终交付**：文件保存到桌面文件夹并告知用户路径后，才算**正式完成交付**。未执行本地归档 = 交付未完成 | 仅列清单不保存 = 用户无法直接使用 = 假交付 |
| **R-IM0** | **启动即配置（R-IM-INIT）**：本 Skill 被 `use_skill` 加载时，**立即执行**以下流程：①扫描环境变量检测已配置哪些生图 API 密钥 → ②**无论是否已配置，都必须在本项目/本对话开始时询问用户选择默认生图模型**。若存在 `.gdpro/preferred-model.json`，可将上次选择作为**推荐项展示，但必须让用户明确确认**，不得自动沿用 → ③若用户所选模型未配置 API 密钥，**立即暂停**并引导配置（读取 `references/image-models.md` §四获取密钥并设置环境变量）。**选择完成后，将该模型写入本项目的 `brand-profile.md`（或 `.gdpro/preferred-model.json`）作为「本项目默认模型」锁定**。**禁止在未确认模型的情况下直接进入设计工作流** | 未确认模型 = 到生图阶段才发现无可用 API 或模型不符合预期 = 流程中断/用户体验断裂 |
| **R-IM0-A** | **项目内模型默认延续规则（R-MODEL-LOCK）**：本项目第一次选择模型后，后续所有生图操作**默认使用该模型**，无需重复询问。只有用户**明确说"切换模型""换个模型""用XX模型"**时，才重新展示模型列表供选择。**禁止在用户未要求切换的情况下擅自更换模型**（包括以"这个模型效果更好"为由自动切换）。**每个新项目/新对话开始时，R-IM0 重新触发，再次询问模型选择** | 擅自切换 = 用户预期外的模型变更 = 风格/质量/价格不一致 = 信任崩塌 |

### 一致性自检（每个物料产出前必过）

```
✅ 品牌名是否与 brand-profile.md 逐字一致？
✅ 色彩是否使用 brand-profile.md 中的 HEX 值？
✅ 字体是否与 brand-profile.md 一致？
✅ 设计哲学是否与 Phase 2 一致？
✅ 品牌口号是否与 brand-profile.md 一致？
✅ 视觉风格（圆角/锐角、留白比例、辅助图形）是否与已确认方向一致？
✅ 中文字号是否≥英文字号？（招牌/广告/包装等有中文+外文并存的物料）
✅ 合规硬约束是否已前置扫描并内化到prompt/设计参数中？
✅ 包装类物料是否同时提供刀图+成品效果图（R10双产出）？
✅ 跨物料重复元素（Logo/品牌名/辅助图形）是否使用预渲染素材而非重绘（R11）？
✅ **用户是否提供了素材但未使用（R15）？**
✅ **当前产出属于「精确排版稿」还是「AI 概念图」？是否走错了路径（R16）？**
✅ **本物料是否超出 AI 生图能力边界？是否需要切换到确定性工具（R18）？**
✅ 食品包装是否预留了强制标签区域？
✅ 色彩是否标注了CMYK和Pantone值（印刷物料）？
✅ **本物料是否已提供可编辑源文件（PSD/AI/EPS/Sketch/Figma），且图层/文字/矢量路径未栅格化（R19）？**
✅ **本物料是否使用了 HTML 截图作为唯一交付物？如果是，必须立即重绘为印刷可用格式（R20）**
✅ **印刷类物料（名片/海报/画册/包装刀图等）是否已添加完整印刷标示线：出血线+裁切标记+套印标记+颜色条+模切/折叠线（R21）？**
✅ **本项目是否已确认默认生图模型？当前生图是否使用了用户未要求切换的模型（R-IM0-A）？**
```

### 输出前5问（融入PM-Clarity自检）

> 每次交付Phase产出前扫描，**任一答案为"否"则必须修正**：

1. ❓ 我理解了用户的真实设计需求吗？（不是表面需求）
2. ❓ 我暴露了关键设计假设吗？（区分了"必须"和"惯例"）
3. ❓ 我在不删除品牌一致性的前提下精简了吗？（没有因为偷懒省略必要物料）
4. ❓ 产出以可执行决策/行动收尾了吗？（不是纯分析或展示）
5. ❓ 用户知道下一步做什么吗？（有明确确认点和后续安排）

### 🛑 追问停止规则（融入my-skill）

**规则A：用户主动喊停** — 用户说"可以了""够了""就这样""没问题" → 立即停止追问，进入产出
**规则B：精确度达标** — Phase 1追问精确度连续2次≥2分（轻量级）或≥3分（深度模式） → 可推进
**规则C：最多轮到达** — 轻量级3轮、中等5轮、深度8轮 → 到达上限先输出【需求简报】，再问"还差什么没聊到？"

**需求简报模板：**
```
📋 设计需求简报（第 X 轮 · 精确度: {score}）

✅ 已确认：
- 品牌名/项目名：xxx
- 核心设计目标：xxx
- 目标受众：xxx
- 调性关键词：xxx

❓ 可能还缺的：
- xxx

还差什么没聊到？
```

---

## 📍 Phase 路由表

> **关键架构变更**：每个Phase的完整指令已拆分到独立文件。进入某个Phase时，读取对应文件获取完整指引，不默认加载全部。

| Phase | 文件 | 何时读取 | 核心产出 |
|-------|------|---------|---------|
| Phase 1 | `references/phase-1.md` | 收到设计需求时 | brand-profile.md |
| Phase 2 | `references/phase-2.md` | 需求确认后 | 设计哲学.md + 品牌战略 |
| Phase 3 | `references/phase-3.md` | 设计哲学确认后 | Logo + 样稿 + 辅助图形 |
| Phase 4 | `references/phase-4.md` | 样稿确认后 | 全套VI物料 |
| Phase 5 | `references/phase-5.md` | 物料全部完成后 | 合规审查报告 |
| Phase 6 | `references/phase-6.md` | 审查通过后 | 落地指导 + VI规范手册PPT + 交付 |
| API/工具 | `references/api-and-tools.md` | 需要调用API/脚本时 | — |

**执行铁律**：
1. 进入任何Phase前，先读取对应的 `references/phase-N.md`
2. 进入任何Phase前，先读取 `brand-profile.md` 确认锁定项（R6）
3. **Skill 启动时，先检查 `.gdpro/` 目录** — 如果存在，读取 `console-integration.md` 并按 R-CINT-1~6 注入 Console 数据（设计师档案 / 知识库 / 项目资产）
4. 每个Phase完成后，暂停等用户确认（R8）
5. 每个Phase产出必须以**可执行决策/行动**收尾，不能以纯分析收尾

### 🔬 Phase 1 Clarify追问法（融入PM-Clarity）

> **不要把错误的需求设计得很漂亮。** Phase 1 追问不只是"问清楚"，要像PM一样追问到不可再分解。

**① 重述表面需求**：用结果语言重述，不只复述用户的框架

| 用户说 | 可能的真实需求 |
|--------|--------------|
| 「我要一个高端Logo」 | 「提升品牌信任感/权威性」 |
| 「设计要大气」 | 「目标场景是什么？户外招牌要远距离可辨？还是名片上要精致？」 |
| 「想要苹果那种风格」 | 「极简+大量留白+无衬线字体+产品为中心」 |

**② 识别模糊用词**：设计领域的常见模糊词：

| 模糊词 | 追问方式 |
|--------|---------|
| 大气 | 大气给谁看？什么让它看起来大气？是留白多？还是字大？ |
| 高端 | 高端的衡量标准是什么？材质？色彩？排版？ |
| 年轻 | 什么年龄段的年轻？二次元年轻还是潮流年轻？ |
| 专业 | 专业给同行看还是给客户看？什么让它看起来专业？ |
| 简约 | 简约到什么程度？极简留白还是信息精简？ |

**③ 区分目标与手段**：
- **手段伪装成目标**：「我要一个3D Logo」→ 真实目标可能是「让品牌看起来有科技感」
- **恐惧伪装成目标**：「颜色不能太素」→ 真实恐惧可能是「怕和竞品混淆」
- **跟风伪装成需求**：「别人都有品牌IP形象」→ 真实需求是什么？IP形象服务于什么？

**④ 暴露初始假设**：这个设计需求成立的前提条件是什么？
- 用户对目标受众的描述有数据支撑吗？
- 「我们的品牌调性是XX」——是经过分析的还是拍脑袋的？
- 对竞品的判断来自观察还是想象？

**⑤ 需求精确度评分**（每轮追问后自评）：

| 分数 | 含义 | 策略 |
|------|------|------|
| 0分 | 模糊、套话（"大气""高端"） | 必须追问，不推进 |
| 1分 | 有方向但粗糙 | +1轮追问补细节 |
| 2分 | 具体但缺边界 | +1轮追问补边界 |
| 3分 | 完整清晰（有受众/场景/调性/参考） | 进入下一阶段 |

> **分数 <2 时，该阶段不计入有效追问轮数。** 轻量级≥2分即可推进，深度模式需≥3分。

**⑥ 重写需求**：给出最锐利版本的真实设计需求

---

### 🎨 产品类设计追问清单（产品渲染模式专用）

> 当模式路由判断为 **🟣 产品渲染模式**（产品效果图/电商主图/详情页/包装产品图/3D渲染）时，在通用追问基础上，**必须追加以下产品访谈**。此清单融入自 `product-demo-generator` 的 PIF（Product Identity Fingerprint）方法论。

**执行顺序**：
1. **参考图预检**（Phase 1 Layer 1 资产预检环节）→ 询问是否有产品草图/照片/竞品图/灵感图
2. **产品身份确认** → 产品名称、类别、核心功能
3. **物理形态** → 形状、尺寸、结构特征
4. **材质与颜色** → 外壳材料、配色、表面处理
5. **细节与品牌** → 屏幕/按钮/接口/Logo/LED/标签
6. **场景与风格** → 背景、灯光、相机角度、氛围
7. **一致性与输出** → 需要多少张渲染图、哪些视角、是否保持外观一致

**追问清单（精简版 15 问，分批进行）：**

| # | 维度 | 追问 | 目的 |
|---|------|------|------|
| P1 | **参考图** | 有产品草图/照片/竞品图/灵感图吗？有的话直接发给我。 | 有参考图 → 图生图模式，跳过已回答的问题 |
| P2 | **产品身份** | 产品名称/型号？属于什么类别？解决什么问题？ | 确定 PIF 基础信息 |
| P3 | **形状** | 整体外形？（圆角矩形/圆柱/有机形态/几何切割） | 锁定产品轮廓 |
| P4 | **尺寸** | 大概多大？（掌心大小/桌面尺寸/手持式） | 确定比例感 |
| P5 | **结构** | 关键结构特征？（单屏+按键/折叠/模块化/一体化） | 锁定结构语言 |
| P6 | **材质** | 外壳/主体材质？（拉丝铝/哑光塑料/玻璃/织物/木质） | 材质决定光影和质感 |
| P7 | **配色** | 主体色+点缀色？或由我推荐？ | 锁定色彩 |
| P8 | **表面** | 表面处理？（亮面/哑光/纹理/透明/金属感） | 影响渲染时的反射特性 |
| P9 | **屏幕** | 有屏幕吗？相对机身大小？边框风格？ | 数字产品/带屏产品必问 |
| P10 | **控件** | 按键/接口/控制件？位置、样式、数量？ | 细节准确性 |
| P11 | **品牌** | Logo 或品牌标识？位置、样式、颜色？ | 品牌一致性 |
| P12 | **指示** | 有 LED/标签/图标/文字吗？ | 细节元素 |
| P13 | **背景** | 背景风格？（纯白棚拍/渐变/环境场景/透明 PNG） | 确定场景 |
| P14 | **灯光** | 灯光偏好？（柔光棚拍/戏剧性/自然光/霓虹） | 确定光影氛围 |
| P15 | **视角** | 需要哪些视角？（正面 45° 主视角/俯拍/侧视/爆炸图） | 确定输出数量和类型 |

**参考图处理规则**：
- 用户上传参考图 → **立即描述图中内容向用户确认** → 提取关键视觉特征（形状/材质/颜色/风格）作为默认值 → 存储路径到 `assets/user-uploads/` → 后续生图强制使用 `image` 参数
- 有参考图的问题可以跳过；无参考图的问题必须追问

**PIF 创建**：
产品访谈完成后，立即创建 `product-fingerprint.md`，写入锁定项。后续所有产品渲染图的 prompt 必须**逐字引用** PIF，不得改写或省略。

```markdown
# Product Identity Fingerprint (PIF)

| 锁定项 | 值 | 确认时间 |
|--------|-----|---------|
| 产品名称 | {值} | Phase 1 |
| 类别 | {值} | Phase 1 |
| 外形 | {值} | Phase 1 |
| 尺寸参照 | {值} | Phase 1 |
| 材质+表面处理 | {值} | Phase 1 |
| 主体色+点缀色 | {值} | Phase 1 |
| 屏幕 | {值} | Phase 1 |
| 控件/接口 | {值} | Phase 1 |
| Logo/品牌标识 | {值} | Phase 1 |
| 背景风格 | {值} | Phase 1 |
| 灯光风格 | {值} | Phase 1 |
| 参考图路径 | {值或无} | Phase 1 |
```

### 🔍 设计决策中的假设管理

> **设计决策不能把假设当事实。** 区分硬约束和软约束。

| 类型 | 定义 | 设计中的例子 |
|------|------|-------------|
| **硬约束** | 不可违反（法规/物理/已锁定品牌） | 《商标法》§10禁用标志、印刷出血3mm、品牌色HEX值 |
| **软约束** | 可挑战的惯例 | 「Logo必须圆形」「行业都用蓝色」「标题必须加粗」 |

> **绝不在没有论证的情况下将软约束呈现为不可变的。** 发现软约束时标注：「这是惯例，不是铁律。如果你愿意挑战，我们可以讨论替代方案。」

**设计假设暴露清单（Phase 2/3产出前自检）：**
- 什么被当成了"必须的"？（如"必须有渐变"）
- 哪个设计选择承载了最大权重？（如Logo的核心识别点）
- 哪个假设如果不成立，设计需要大改？（如假设受众是年轻人→实际是中年人）

---

## 🔧 Skill 自进化机制（R-E1 ~ R-E7）

> **用户纠正流程偏差时，必须自动迭代规则文件，不需要用户明确要求"改skill"。** 详见 `references/skill-self-evolution.md`

| # | 规则 | 违反后果 |
|---|------|---------|
| R-E1 | **纠正即触发**：用户纠正流程/工具/参数偏差 = 立即触发自进化 | 忽略纠正 = 违规 |
| R-E2 | **三步进化**：捕获纠正→诊断根因→固化到规则文件，三步不可跳过 | 跳过固化 = 违规 |
| R-E3 | **具体可执行**：固化内容必须是具体指令，禁止"注意""确保"等模糊措辞 | 模糊修正 = 无效 |
| R-E4 | **进化可追溯**：每次进化写入 SKILL.md 末尾「进化日志」+ CHANGELOG.md | 不记录 = 违规 |
| R-E5 | **重复错误升级**：同一问题纠正2次=升级规则硬度，3次=升级为铁律 | 不升级 = 违规 |
| R-E6 | **禁止"知道了但没改"**：说"记住"的同时必须已完成文件修改 | 只说不改 = 违规 |
| R-E7 | **跨场景泛化**：从具体纠正推导通用规则，覆盖同类场景 | 仅修个案 = 不充分 |

### 🔄 Reflect 7步法（融入my-skill）

当用户纠正或设计产出被否定时，自动进入反思流程：

| 步骤 | 动作 | 说明 |
|------|------|------|
| 1. Identify | 定位根因 | 哪个Phase出了问题？是指令缺失/有歧义/参考过时/描述太窄？ |
| 2. Read | 重读原文 | 重读相关SKILL.md和phase-N.md，确认gap存在（别靠记忆） |
| 3. Impact scan | 跨文件影响 | 搜索其他phase-N.md和references/是否引用同一概念 |
| 4. Determinism | 确定性选择 | 能写成固定规则的→改SKILL.md/phase-N；需要LLM判断的→保留指令+加示例 |
| 5. Propose | 提出方案 | 改哪个文件+加什么/删什么/改什么+为什么能防止复发 |
| 6. Confirm | 等待同意 | 向用户展示方案，等明确同意后再改 |
| 7. Apply | 执行修改 | 修改文件 + 升级版本号 + 写进化日志 + 写CHANGELOG.md |

**升级警告：**
- 同一问题连续反思2次 → 停止打补丁，考虑根本方案是否有误
- 不同问题累计3+次反思 → 结构性问题，考虑是否需要拆分Phase或重组规则

### 📐 位置四问（融入huashu-design）

> **每个物料/页面/镜头开工前必答。这一步的前半段比所有设计参数更决定输出质量。**

| 问题 | 含义 | 示例 |
|------|------|------|
| **叙事角色** | 这个物料在品牌故事中扮演什么角色？ | hero（主角）/ 过渡 / 数据 / 引语 / 结尾？ |
| **观众距离** | 观众在什么距离看这个物料？ | 10cm 手机 / 1m 笔记本 / 10m 户外招牌？ |
| **视觉温度** | 这个物料要传达什么情绪？ | 安静 / 兴奋 / 冷静 / 权威 / 温柔 / 悲伤？ |
| **容量估算** | 内容塞得下吗？ | 用纸笔画3个5秒 thumbnail 估算，防溢出/防挤压 |

**执行规则**：四问答完再确定色彩/字体/布局节奏——系统服务于答案，不是先选系统再塞内容。

---

### 🎯 5维度专家评审框架（融入huashu-design，与design-critique互补）

> **当用户说"评审"/"好不好看"/"review"/"打分"时，或你主动想质检时，按此框架输出结构化评审报告。**
>
> **与 design-critique 的关系**：design-critique 是 11 维度的专业设计理论评审（格式塔/色彩/排版/网格等），5维度框架是**面向用户决策的评分工具**——哲学一致性/视觉层级/细节执行/功能性/创新性各 0-10 分，附带可执行的修复清单。

#### 5维度评分标准

| 维度 | 9-10分标准 | 1-2分标准 |
|------|-----------|----------|
| **哲学一致性** | 每个细节都有 Phase 2 设计哲学的依据 | 与确立的设计哲学基本无关 |
| **视觉层级** | 视线自然沿设计者意图流动，零摩擦 | 混乱，用户不知道先看哪里 |
| **细节执行** | 像素级精确，对齐/间距/颜色无瑕疵 | 粗糙，看起来像草稿 |
| **功能性** | 每个元素都服务于目标，零冗余 | 完全被装饰淹没，失去传达能力 |
| **创新性** | 在该哲学框架内找到了独特表达 | 完全是模板或素材拼凑 |

#### 场景侧重（不同物料不同重点）

| 场景 | 最重要维度 | 次重要 | 可放宽 |
|------|-----------|--------|--------|
| 海报/封面/社交媒体图 | 创新性、视觉层级 | 哲学一致性 | 功能性（单图无交互） |
| 名片/信纸/办公物料 | 细节执行、功能性 | 视觉层级 | 创新性（专业优先） |
| 包装 | 功能性、视觉层级 | 细节执行 | 创新性（准确优先） |
| PPT/品牌手册 | 视觉层级、功能性 | 细节执行 | 创新性（清晰优先） |
| Logo | 哲学一致性、创新性 | 细节执行 | 功能性（标识优先） |

#### 评审输出模板

```markdown
## 设计评审报告

**总体评分**：X.X/10 [优秀(8+)/良好(6-7.9)/需改进(4-5.9)/不合格(<4)]

**分项评分**：
- 哲学一致性：X/10 [一句话说明]
- 视觉层级：X/10 [一句话说明]
- 细节执行：X/10 [一句话说明]
- 功能性：X/10 [一句话说明]
- 创新性：X/10 [一句话说明]

### 优点（Keep）
- [具体指出做得好的地方]

### 问题（Fix）
[按严重程度排序]

**1. [问题名称]** — ⚠️致命 / ⚡重要 / 💡优化
- 当前：[描述现状]
- 问题：[为什么这是问题]
- 修复：[具体操作，含数值]

### 快速修复清单（Quick Wins）
如果只有5分钟，优先做这3件事：
- [ ] [最有影响力的修复]
- [ ] [第二重要的修复]
- [ ] [第三重要的修复]
```

**评审原则**：评审设计，不评设计师。问题描述聚焦「设计本身」，不评价设计师能力。

---

### 🖼️ 素材质量门槛「5-10-2-8」原则（融入huashu-design）

> **Logo 除外：有就必须用。其他素材（产品图/参考图/配图）遵循此门槛。**
>
> 原则：搜索5轮 → 找到10个候选 → 精选2个 → 每个8/10分以上。宁可少一些，也不滥竽充数。

| 维度 | 标准 | 反模式 |
|------|------|--------|
| **5 轮搜索** | 多渠道交叉搜（官网/press kit/官方社媒/YouTube截帧），不是一轮抓前2个就停 | 第一页结果直接用 |
| **10 个候选** | 至少凑10个备选才开始筛 | 只抓2个，没得选 |
| **选 2 个好的** | 从10个里精选2个作为最终素材 | 全都用 = 视觉过载+品位稀释 |
| **每个 8/10 分以上** | 不够8分**宁可不用**，用诚实placeholder或AI生成（以官方参考为基底） | 凑数7分素材进项目 |

**8/10 评分维度**：
1. **分辨率** · ≥2000px（印刷/大屏≥3000px）
2. **版权清晰度** · 官方来源 > 公共领域 > 免费素材 > 疑似盗图（0分）
3. **与品牌气质契合度** · 和 brand-profile.md 里的「气质关键词」一致
4. **光线/构图/风格一致性** · 2个素材放一起不打架
5. **独立叙事能力** · 能单独表达一个叙事角色（不是装饰）

**为什么这个门槛是铁律**：
- 滥竽充数的素材比没有更糟——污染视觉品味、传递「不专业」信号
- 消费者看作品时，每个视觉元素都在**积分或扣分**
- 7分素材 = 扣分项，不如留空或用诚实placeholder

---

## 🎨 审美学习与风格固化（R-A1 ~ R-A10）

> **用户表达审美偏好时，必须捕获→翻译→固化到品牌审美档案，后续产出自动对齐。** 详见 `references/aesthetic-learning.md`

| # | 规则 | 违反后果 |
|---|------|---------|
| R-A1 | **偏好即时捕获**：用户表达的审美偏好必须立即捕获并分类 | 漏捕获 = 风格漂移 |
| R-A2 | **偏好翻译**：模糊感受必须翻译为具体可执行视觉参数 | 不翻译 = 不可执行 |
| R-A3 | **固化到审美档案**：确认后的偏好立即写入 brand-profile.md 审美档案 | 只在对话记 = 无效 |
| R-A4 | **审美档案驱动产出**：后续物料必须主动应用审美档案偏好 | 不应用 = 风格漂移 |
| R-A5 | **冲突检测**：新偏好 vs 旧偏好/锁定项冲突时，输出冲突声明 | 忽略冲突 = 自相矛盾 |
| R-A6 | **审美纠偏闭环**：纠正→翻译→固化→更新禁止→验证，闭环不可断 | 断闭环 = 偏好不固化 |
| R-A7 | **编号追溯**：审美档案每条偏好有AP编号 | 无编号 = 不可追溯 |
| R-A8 | **参考作品接收分类**：用户投喂图片必须先分类再提取特征 | 不分类 = 权重错乱 |
| R-A9 | **审美特征提取**：从参考作品中用color-palette+视觉分析提取7维度特征 | 不提取 = 浪费参考 |
| R-A10 | **参考作品固化归档**：提取的特征→对比→确认→写入+归档+生成伴生.md | 不归档 = 不可追溯 |

---

## 参考资料体系

| 文件 | 覆盖内容 | 何时读取 |
|------|---------|---------|
| `references/phase-1.md` | 需求追问详细指引 + brand-profile模板 | Phase 1 |
| `references/phase-2.md` | 竞品分析 + brand-cog + Moodboard + 设计哲学 | Phase 2 |
| `references/phase-3.md` | 豆包API + Prompt构造 + 画布增强 + 设计评审 + 辅助图形 + Logo绘制 | Phase 3 |
| `references/phase-4.md` | 品牌套件 + VI物料清单 + VI刚性规范 + 执行策略 + 色彩系统 | Phase 4 |
| `references/phase-5.md` | 合规审查 + 审美自检 + 审查清单 + 审查输出格式 | Phase 5 |
| `references/phase-6.md` | 印刷前检查 + 数字检查 + VI维护 + 交付物清单 | Phase 6 |
| `references/api-and-tools.md` | API配置 + CellCog + 输出处理 + 脚本说明 | 需要时 |
| `references/image-models.md` | 10家16+生图模型全览 + 参数 + 定价 + 接入指南 + 调用代码 + 月度扫描机制 | **每次生图前必读** |
| `references/ai-image-limitations.md` | AI生图能力边界·技术原理·漂移根因·3D结构限制·Prompt真相·场景路径推荐·自检清单 | **每次使用AI生图前必读（R18）** |
| `references/design-compliance.md` | 14行业合规·12类Checklist·食品标签·3C认证·包装刀图·商场规范 | R9合规前置 |
| `references/design-aesthetics.md` | 审美参照·Logo5原则·格式塔·Itten色彩·网格·案例·自检 | Phase 2/5 |
| `references/anti-ai-slop.md` | 反AI Slop完整清单·视觉/字体/色彩/Layout陷阱·决策速查 | **每次设计前必读** |
| `references/taste-anchors.md` | 品位锚点·字体配对·色彩策略·信息密度分型·细节签名·Scale规范 | Phase 2/3 |
| `references/design-philosophies.md` | 13种设计哲学风格库·生图提示词DNA·MoodNotLayout原则 | Phase 2 |
| `references/frontend-design-aesthetics.md` | 前端审美指南·字体/色彩/动效/构图/背景细节·数字物料检查清单 | 数字物料设计时 |
| `references/algorithmic-art-guide.md` | 算法艺术指南·辅助图形/纹理生成·噪声/流场/粒子/递归技术 | 辅助图形/纹理生成时 |
| `references/external-resources.md` | 外部设计资源索引·灵感/配色/图片/插画/字体/图标/工具 | 素材搜索时参考 |
| `references/vi-materials.md` | 100+物料清单·Logo刚性规范(H节)·色彩标注·字体层级 | Phase 4 |
| `references/vi-design-toolkit.md` | 辅助图形·字体决策树·竞品分析·Moodboard·色彩比例·VI维护·落地 | Phase 2/4/6 |
| `references/material-guide.md` | 按品类的材质/工艺/价格决策指南·包装/印刷/招牌·UV/烫金/覆膜速查 | **用户无法明确材质时必读（R22）** |
| `references/skill-self-evolution.md` | R-E1~R-E7·纠正即触发·三步进化·进化日志 | 纠正偏差时 |
| `references/aesthetic-learning.md` | R-A1~R-A10·偏好捕获翻译固化·审美档案·参考作品投喂 | 审美偏好时 |
| `references/design-references.md` | 9品牌摘要索引·VI手册结构·标志规范·色彩系统·字体系统·禁令清单·设计原则·国际方法论·39个设计网站 | Phase 1/2/4 参考 |
| `references/brand-cases/*.md` | 各品牌完整VI知识（可口可乐/天猫/MOMO等） | **按需读取**，参考design-references.md索引 |
| `references/console-integration.md` | Console 数据互通协议·.gdpro 格式规范·设计师档案/知识库/资产同步规则 | **Skill 启动时必读**（如果检测到 .gdpro/ 目录） |

### 🔁 知识库自动扫描规则（R-KB）

> **机制**：design-references.md 头部包含 `🕐 最后扫描时间` 时间戳
>
> **触发逻辑**：
> 1. **Skill首次启动** → 检查时间戳 → 如果不存在或距当前时间 > **14天** → 触发全站扫描
> 2. **用户频繁使用**（14天内使用过）→ 每 **14天** 自动扫描一次
> 3. **用户长期未用**（距上次使用 > 14天）→ 下次启动时 **立即扫描**
>
> **扫描内容**：design-references.md §八-B 中标记为 `—`（未扫描状态）的网站
> - 使用 web_fetch 工具逐站抓取首页/核心页面
> - 提取有价值的设计方法论/案例/趋势信息
> - 追加到对应章节，更新该站的"扫描状态"
> - 扫描完成后更新 `🕐 最后扫描时间` 为当前ISO时间
>
> **时间戳格式**：`YYYY-MM-DDTHH:MM:SS` (ISO 8601)

### 📦 内容增长控制规则（R-CG）

> **核心问题**：知识库每次扫描/品牌识别都会增加内容，如不加控制会超出上下文窗口导致规则漏执行。

| # | 规则 | 违反后果 |
|---|------|---------|
| R-CG1 | **索引+分文件架构**：design-references.md 仅保留摘要索引（≤500行），品牌详情拆到 `brand-cases/` 独立文件 | 单文件过大 = 注意力稀释 |
| R-CG2 | **按需读取**：Phase 进入时只读索引，需要具体品牌知识时再读取 brand-cases/ 中的文件 | 全量读取 = 上下文爆炸 |
| R-CG3 | **扫描内容摘要化**：网站扫描提取的信息，每站最多200字摘要写入 design-references.md，详细内容存入 `brand-cases/scanned-sites.md` | 原文照搬 = 膨胀失控 |
| R-CG4 | **brand-profile.md 控制增长**：审美档案每条≤1行；参考作品归档保存文件路径而非嵌入内容；伴生.md存在 `aesthetic-references/` 目录 | 嵌入内容 = 文件膨胀 |
| R-CG5 | **定期瘦身**：每次扫描后检查 design-references.md 行数，>500行时触发瘦身（详细内容迁移到 brand-cases/ 或 scanned-sites.md） | 不瘦身 = 渐进失效 |
| R-CG6 | **审美档案归档**：项目完成后，brand-profile.md 中>10条AP的审美档案归档到 `aesthetic-references/archive-{品牌名}.md`，保留最新10条 | 档案过长 = 每次读取浪费 |

### 🎨 生图模型选择规则（R-IM）

> **核心原则**：不默认使用任何一家API，每次生图必须让用户选择。覆盖11家18+模型。

| # | 规则 | 违反后果 |
|---|------|---------|
| R-IM1 | **首次使用必须询问**：用户第一次进入生图阶段时，读取 `references/image-models.md`，列出所有可用模型+状态+优劣+价格，让用户选择 | 默认使用 = 违规 |
| R-IM2 | **检测环境变量**：生图前先检测哪些API密钥已配置（见image-models.md §三），标注"✅可用"和"❌未配置" | 不检测 = 盲目调用 = 失败 |
| R-IM3 | **引导配置**：用户选择未配置的模型时，引导其按 image-models.md §四获取密钥并设置环境变量 | 跳过引导 = 用户卡住 |
| R-IM4 | **每次生图可切换**：用户可在任意时刻切换生图模型，不锁定单一模型。但**默认行为是延续本项目首次选择**（R-IM0-A），只有用户主动要求时才切换 | 强制锁定 = 限制灵活性；擅自切换 = 预期外变更 |
| R-IM5 | **场景推荐**：当用户不确定选哪个时，根据设计场景（Logo/海报/电商/概念图等）推荐最适合的模型 | 不推荐 = 用户迷失 |
| R-IM6 | **调用参数必须准确**：每个模型的size/quality/model名等参数严格按 image-models.md 中的文档，禁止猜测 | 猜测参数 = 调用失败 |
| R-IM7 | **月度扫描更新**：技能启动时检查image-models.md头部日期，距上次扫描>30天→自动触发§七的10站扫描，更新模型/价格/停服信息 | 不扫描 = 信息过时 |
| R-IM8 | **老模型清理**：当新版本取代旧版本时（如Seedream 4.5取代4.0），从总览移除旧版，仅保留最新1~2个版本 | 保留老模型 = 误导用户 |
| R-IM9 | **产品渲染一致性链路（R-CHAIN）**：产品渲染/效果图/电商主图涉及**同一产品的多视角/多场景输出**时，必须使用**一致性链路**：①首图用文生图或图生图生成 → 用户确认后 → ②第2张及后续所有图必须**以「已确认的首图」为参考图**（`image` 参数）生成，而非重新文生图 → ③每张新图继续以上一张已确认图为参考。**PIF 必须在每张图的 prompt 中逐字完整引用**。一致性链路最多 3–5 张，超过后需重新以首图为参考开启新链路，防止漂移累积。`scale` 参数建议 0.3–0.4 以保持产品外观高度一致 | 不用一致性链路 = 同一产品的不同视角看起来不像同一个产品 = 电商/包装无法使用 |
| R-IM10 | **三种生图方式强制声明（R-THREE-MODES）**：每次进入生图阶段前，必须向用户明确说明三种可用方式，并让用户选择：①**文生图**（纯文字描述，适合概念探索/无参考图）②**图生图**（文字+参考图，适合基于草图/照片/竞品优化）③**多组图生成**（一次生成多张连贯图/多角度，适合批量产出/故事板）。**禁止默认使用文生图而不告知其他选项**。用户选择后，按 image-models.md 对应模型的 API 参数执行 | 不告知选项 = 用户不知道可以图生图或多组图 = 限制产出可能性 |

## 技能协作体系

| 技能 | 用途 | 调用时机 |
|------|------|---------|
| canvas-design | 设计哲学方法论+81字体+画布排版 | Phase 2设计哲学 + Phase 3精细排版 |
| brand-cog | 品牌战略方法论 | Phase 2品牌战略定义 |
| design-critique | 11维度设计评审 | Phase 3样稿评审 |
| ui-design-review | 10维度视觉评审+0-10评分 | Phase 3补充评审 |
| svg-draw | SVG矢量绘制+PNG转换 | Phase 3 Logo + 辅助图形绘制 |
| color-palette | 图片色彩提取 | Phase 2 Moodboard + Phase 4色彩提取 |
| **pptx** | **PPT演示文稿生成** | **Phase 6 VI规范手册PPT交付** |
| frontend-design（方法论融入） | 前端审美·动效·空间构图·背景细节·反AI slop | 数字物料/Banner/网页配图时 |
| algorithmic-art（方法论融入） | 算法美学·辅助图形生成·噪声/流场/粒子/递归 | 辅助图形/包装纹理/动态元素时 |

---

## 关键原则

1. **先追问，后生成** — 不急于出图，先确保需求清晰（Phase 1 Clarify追问法）
2. **品牌一致性铁律** — R1-R14一旦确认，全流程锁死
3. **品牌战略先行** — 品牌类设计先定义定位和个性，再谈视觉
4. **竞品分析必做** — 品牌类设计必须先分析竞品视觉
5. **设计哲学驱动** — 每套设计都有统一的美学纲领
6. **样稿确认再扩展** — 避免方向错误导致大量返工
7. **合规内置** — 生成时即扫描（R9），不是事后补审
8. **版权红线** — 字体版权、商标法§10、AI著作权风险，零容忍
9. **风格统一** — 所有物料共享同一视觉基因
10. **审美自检** — 每个产出都要通过审美质量自检清单
11. **辅助图形不缺席** — 辅助图形是VI基础系统的必要组件
12. **色彩有比例** — 主色60%+辅助色30%+强调色10%
13. **落地闭环** — 必须提供印刷/数字检查清单和VI维护指南
14. **输出实用** — 矢量/PSD/AI/EPS可编辑源文件是交付底线，不是只能看的位图
15. **工艺极致** — 每件作品都应看起来经过无数小时精心制作
16. **禁止自由发挥** — 品牌确认的内容是铁律，不是"参考"
17. **解释为什么，不堆砌MUST** — 给理由比下命令更有效，LLM有推理能力（融入my-skill）
18. **假设必须暴露** — 设计决策中的"惯例"必须标注可挑战，不能把假设当事实（融入PM-Clarity）
19. **模式路由** — 不在简单项目上做重型流程，按复杂度选择深度（融入PM-Clarity）
20. **复利积累** — 每个项目的审美反馈都让下一个项目更好（融入my-skill）
21. **核心资产 > 规范** — Logo/产品图/UI截图的真实资产比色值字体更重要，是品牌被认出来的根基（融入huashu-design）
22. **验证省返工** — 交付前最少1分钟的验证可以省1小时的返工（融入huashu-design）
23. **宁缺毋滥** — 7分素材不如留空，滥竽充数的素材污染视觉品味（融入huashu-design）
24. **产品先访谈，后渲染** — 产品渲染必须先过 15 问产品访谈并锁定 PIF，禁止直接凭想象生图（融入product-demo-generator）
25. **一致性链路保产品** — 同一产品的多视角渲染必须使用一致性链路，以首图为参考逐张生成，防止产品外观漂移（融入product-demo-generator）
26. **不知道就问，不给选项不动手** — 用户无法明确材质/工艺/品类时，必须提供选项+理由+价格，禁止跳过决策（R22）

---

## 🔐 渐进式披露协议（Progressive Disclosure Protocol）

> **核心思路：不一次性暴露全部信息，按「当前必要→确认后深入→高级按需」三层递进，确保每一步的决策质量。**
>
> 这是保证执行精度和出品质量的根本机制。违反本协议 = 信息过载 → 注意力稀释 → 规则漏执行 → 出品降质。

### 三层披露模型

```
┌─────────────────────────────────────────────────────────────┐
│  Layer 1 · 表面层（Surface）                                 │
│  → 只给「此刻必须知道」的信息                                 │
│  → 让用户做「方向性」决策，不做「细节性」决策                  │
│  → 禁止提前暴露下层内容                                       │
├─────────────────────────────────────────────────────────────┤
│  Layer 2 · 细节层（Detail）  ← 用户确认/执行完 Layer 1 后解锁 │
│  → 暴露「执行所需的精确参数和约束」                           │
│  → 让用户做「细节性」决策                                     │
│  → 仍禁止暴露 Layer 3                                        │
├─────────────────────────────────────────────────────────────┤
│  Layer 3 · 高级层（Advanced） ← 用户主动要求或场景触发时解锁   │
│  → 暴露「边缘情况、备选方案、深度定制选项」                    │
│  → 不影响主线流程的完整性                                     │
└─────────────────────────────────────────────────────────────┘
```

### 强制检查点（Checkpoint Gates）

> 每个 Phase 内部按 Layer 推进，**Layer 之间必须过 Gate**。

| Gate | 触发条件 | 不过 Gate 的后果 |
|------|---------|----------------|
| **G1 方向确认门** | Layer 1 展示后 | 用户未确认方向 → 禁止进入 Layer 2 |
| **G2 细节确认门** | Layer 2 展示/执行后 | 用户未确认细节 → 禁止进入 Layer 3 或下一 Phase |
| **G3 产出验收门** | 每个 Phase 最终产出后 | 用户未验收 → 禁止推进到下一 Phase |
| **G4 一致性闸门** | 进入任何 Phase 时 | 未读取 brand-profile.md 确认锁定项 → 禁止开始该 Phase |

**Gate 执行句式（强制执行）：**
```
「{当前完成的内容} 已就绪。请确认以下两点后再继续：
  1. {方向/细节} 是否满意？（满意/需修改）
  2. 是否推进到 {下一步}？（是/否）」
```

### Phase 内分层执行规则

| 规则 | 违反后果 |
|------|---------|
| **PD-1** | 进入 Phase 时，只加载该 Phase Layer 1 的内容 | 预加载全部 = 注意力稀释 |
| **PD-2** | Layer 1 执行完毕且用户明确确认后，才解锁 Layer 2 | 未确认就解锁 = 决策基础不牢 |
| **PD-3** | Layer 2 执行完毕且用户明确确认后，才解锁 Layer 3 | 同上 |
| **PD-4** | 用户说"先做出来看看"时，只执行最小可行版本（Layer 1 的极速版），而非跳过披露 | 跳过披露 = 返工风险 |
| **PD-5** | 每个 Layer 的输出必须以「可执行决策」收尾，不能以「仅供参考」收尾 | 无决策点 = 流程悬空 |
| **PD-6** | 禁止在 Layer 1 中提及 Layer 2/3 的存在（不预告未解锁内容） | 预告 = 认知负荷前置 |
| **PD-7** | 跨 Phase 时，前一 Phase 的 Layer 3 内容不自动带入下一 Phase | 自动带入 = 信息膨胀 |

### brand-profile.md 渐进式字段解锁

品牌档案字段不按 Phase 预填，而是**按 Layer 逐步解锁写入**：

| Phase | Layer 1 解锁字段 | Layer 2 解锁字段 | Layer 3 解锁字段 |
|-------|-----------------|-----------------|-----------------|
| Phase 1 | 品牌中英文名称 | 品牌口号、行业标签 | 商标注册状态、字体授权清单 |
| Phase 2 | 设计哲学名称 | 设计哲学阐述、品牌战略 | Moodboard 参考归档 |
| Phase 3 | 主色 HEX | 完整色彩系统、字体系统 | Logo 变体清单、辅助图形策略 |
| Phase 4 | 物料清单确认 | 各物料源文件路径 | 印刷工艺备注、供应商建议 |
| Phase 5 | 合规审查摘要 | 详细审查报告 | 风险等级评估、整改优先级 |
| Phase 6 | 交付物清单 | 各文件路径+格式 | 维护指南、版本管理策略 |

**字段未解锁时，brand-profile.md 中显示为 `🔒 待 Phase X Layer Y 解锁`，禁止提前填写。**

### 与渐进式加载的区别

| 维度 | 渐进式加载（原机制） | 渐进式披露（新增机制） |
|------|---------------------|----------------------|
| 解决什么问题 | 文件太大，上下文装不下 | 信息太多，决策质量下降 |
| 控制粒度 | 文件级（按需读取文件） | 指令级（按需暴露细节） |
| 用户感知 | 无感知（后台加载） | 有感知（分层确认） |
| 核心目标 | 避免上下文溢出 | 保证每一步出品精度 |

---

## 📐 渐进式加载原则（融入my-skill）

> **核心思路：SKILL.md是路由表+硬规则，Phase指令和参考资源按需加载。**

| 层级 | 内容 | 大小 | 何时加载 |
|------|------|------|---------|
| 元数据 | name + description | ~200字 | 始终在上下文中 |
| 主控 | SKILL.md正文（路由+铁律+规则） | ~400行 | Skill触发时 |
| Phase指令 | references/phase-N.md | 各100-200行 | 进入对应Phase时 |
| 参考资源 | references/design-*.md, brand-cases/ | 不限 | 按需读取 |

**关键实践：**
- 大参考文件(>300行)要加目录
- 品牌案例按需读取，不默认加载全部9个
- Phase指令只在进入时读取，不预加载
- **新增：Phase 指令内部按 Layer 递进披露，不一次性暴露全文件内容**

---

---

## 🎯 核心资产协议（R-BRAND-ASSET）

> **涉及具体品牌时强制执行。这是稳定性的生命线，直接决定输出质量是40分还是90分。**
> 完整 5 步流程见 `references/brand-spec.md`。
>
> 品牌的本质是「它被认出来」。按识别度排序：

| 资产类型 | 识别度贡献 | 必需性 |
|---|---|---|
| **Logo** | 最高 | **任何品牌都必须有** |
| **产品图/产品渲染图** | 极高 | **实体产品必须有** |
| **UI 截图/界面素材** | 极高 | **数字产品必须有** |
| **色值** | 中 | 辅助 |
| **字体** | 低 | 辅助 |

**执行规则**：
- 只抽色值 + 字体、不找 logo / 产品图 / UI → **违反本协议**
- 用 CSS 剪影/SVG 手画替代真实产品图 → **违反本协议**
- 找不到资产不告诉用户、硬做 → **违反本协议**
- 宁可停下问用户要素材，也不要用 generic 填充

### 5 步硬流程（摘要版）

| 步骤 | 动作 | Fallback |
|------|------|----------|
| **Step 1 · 问** | 按优先级清单逐项问用户要资产 | — |
| **Step 2 · 搜** | 官网 brand/press 页、App Store、inline CSS/SVG | — |
| **Step 3 · 下载** | Logo(SVG→inline→avatar) / 产品图(hero→press kit→AI兜底) / UI(商店截图→官网截图) | AI 生成兜底 |
| **Step 4 · 验证** | Logo(可打开+双版本+透明) / 产品图(2000px+干净背景) / UI(真实分辨率+最新版) / 色值(警惕示范品牌污染) | — |
| **Step 5 · 固化** | 写入 `brand-spec.md`，所有产出引用真实资产路径 | — |

简化速记版：

```markdown
# <Brand> · Brand Spec
> 采集日期：YYYY-MM-DD
> 资产来源：<列出下载来源>
> 资产完整度：<完整 / 部分 / 推断>

## 🎯 核心资产（一等公民）

### Logo
- 主版本：`assets/<brand>-brand/logo.svg`
- 浅底反色版：`assets/<brand>-brand/logo-white.svg`
- 使用场景：<片头/片尾/角落水印/全局>
- 禁用变形：<不能拉伸/改色/加描边>

### 产品图（实体产品必填）
- 主视角：`assets/<brand>-brand/product-hero.png`（2000×1500）
- 细节图：`assets/<brand>-brand/product-detail-1.png`
- 场景图：`assets/<brand>-brand/product-scene.png`

### UI 截图（数字产品必填）
- 主页：`assets/<brand>-brand/ui-home.png`
- 核心功能：`assets/<brand>-brand/ui-feature-<name>.png`

## 🎨 辅助资产

### 色板
- Primary: #XXXXXX <来源标注>
- Background: #XXXXXX
- Ink: #XXXXXX
- Accent: #XXXXXX
- 禁用色: <品牌明确不用的色系>

### 字型
- Display: <font stack>
- Body: <font stack>

### 气质关键词
- <3-5 个形容词>

### 禁区
- <明确不能做的>
```

**写完 spec 后的执行纪律（硬要求）**：
- 所有产出必须**引用** `brand-spec.md` 里的资产文件路径，不允许用 CSS 剪影/SVG 手画代替
- Logo 作为 `<img>` 引用真实文件，不重画
- 产品图作为 `<img>` 引用真实文件，不用 CSS 剪影代替
- 这让品牌一致性从「靠自觉」变成「靠结构」——想临时加色要先改 spec

### 全流程失败的兜底

| 缺失 | 处理 |
|---|---|
| **Logo 完全找不到** | **停下问用户**，不要硬做 |
| **产品图（实体产品）找不到** | 优先 AI 生成（以官方参考图为基底）→ 次选向用户索取 → 最后诚实 placeholder |
| **UI 截图（数字产品）找不到** | 向用户索取自己账号的截屏 → 官方演示视频截帧 |
| **色值完全找不到** | 按设计方向顾问模式，推荐 3 个方向并标注 assumption |

**协议代价 vs 不做代价**：

| 场景 | 时间 |
|---|---|
| 正确走完协议 | 下载 logo 5 min + 下载产品图 10 min + 提取色值 5 min + 写 spec 10 min = **30 分钟** |
| 不做协议的代价 | 做出没识别度的通用设计 → 用户返工 1-2 小时，甚至重做 |

---

## 📋 进化日志

> 本区域保留最新 3 条进化记录。完整历史见 `references/evolution-log.md`。
> 每次 R-E1 触发后，在 `references/evolution-log.md` 追加条目，并更新本区域保留最新 3 条。

### E-021: 最终交付物本地归档 — 桌面文件夹 + 分层保存

| 字段 | 内容 |
|------|------|
| 日期 | 2026-04-26 |
| 触发原文 | "形成用户的产出物之后，在桌面生成一个文件夹，并把用户最后确认、明确表示采用的产物全部下载存到这个文件夹中，供用户后续直接使用" |
| 根因 | 1) 原有流程仅列出交付物清单，未强制保存到用户本地；2) 对话结束后用户可能找不到文件；3) 中间稿/草稿与最终确认稿未区分，容易混淆；4) 无明确"交付完成"标志，存在"假交付"风险 |
| 修正位置 | SKILL.md 新增 R23+R23-A + 版本号 v2.13.0；phase-6.md P6-G3 最终交付门补充归档流程 |
| 修正内容 | R23 强制桌面归档（`Desktop\{品牌名}_交付物_YYYYMMDD\`，分 01_源文件/02_预览文件/03_规范文档）；R23-A 归档即交付；Phase 6 新增最终确认→明确采用→归档保存三步流程 |
| 影响范围 | Phase 6 交付环节（全项目最终交付） |
| 验证状态 | 🔄 待验证 |

### E-020: Console 启动从"自动"改为"询问偏好+按需启动"

| 字段 | 内容 |
|------|------|
| 日期 | 2026-04-26 |
| 触发原文 | "通过对话界面来调用还是通过图形界面来调用，应该要在使用前去询问用户" / "为什么我打开这个 Skill 就直接给我启动了图形界面呢？" |
| 根因 | 1) 原 R-T6 规定 skill 加载时自动启动 Console，用户被迫使用图形界面，打断原有对话习惯；2) 用户可能更习惯在对话界面中快速沟通和确认，自动启动 Console 是侵入式行为；3) 未给用户选择权的自动启动 = 不尊重用户偏好 |
| 修正位置 | SKILL.md 修改 R-T6 + 修改 R-T7 + Console 执行规则修正 + 版本号 v2.12.0 |
| 修正内容 | R-T6 从"自动启动 Console"改为"使用方式偏好询问"（对话界面 vs 图形界面）；R-T7 从"自动检测启动"改为"按需启动"（只有用户明确输入关键词时才启动）；去掉"技能被加载时"的自动启动描述 |
| 影响范围 | Skill 启动流程（R-T6/R-T7）+ 用户首次体验 |
| 验证状态 | 🔄 待验证 |

### E-019: 启动模型选择流程修正 — 必须询问+确认，项目内默认延续，禁止擅自切换

| 字段 | 内容 |
|------|------|
| 日期 | 2026-04-26 |
| 触发原文 | "启动时是不是会先询问用户调用哪个模型...后面一直延续第一次选择的模型...每个对话框中同一个项目的第一次选择...新项目还是要再次询问" |
| 根因 | 1) 原 R-IM0 允许"若已配置且存在 preferred-model.json，直接采用上次选择"，导致新项目可能自动沿用旧模型而不询问用户；2) 缺少"项目内默认延续、禁止擅自切换"的硬规则；3) 执行者可能以"另一个模型效果更好"为由擅自更换模型，用户预期外变更 |
| 修正位置 | SKILL.md 修改 R-IM0 + 新增 R-IM0-A + 修改 R-IM4 + 一致性自检清单新增 + 版本号 v2.11.0 |
| 修正内容 | R-IM0 改为「无论是否已配置，都必须询问+确认」；新增 R-IM0-A「项目内模型默认延续，用户未说切换不得擅自更换」；R-IM4 补充默认延续说明；自检清单新增模型确认项 |
| 影响范围 | 启动流程（R-IM0）+ 全项目生图环节（R-IM0-A/R-IM4） |
| 验证状态 | 🔄 待验证 |

> 📜 更早记录（E-001~E-018）→ `references/evolution-log.md`

---

## 📌 版本记录

> 📌 **当前版本：v2.13.0** （2026-04-26）
> 📋 完整变更历史见 [CHANGELOG.md](./CHANGELOG.md)

### 版本号规则（融入my-skill）

| 变更类型 | 示例 | 说明 |
|---------|------|------|
| **小修/补细节** | v2.0.0 → v2.0.1 | 改错别字、补漏、微调措辞 |
| **新增功能/改流程** | v2.0.1 → v2.1.0 | 加了新规则、新Phase、新能力 |
| **重构/大改方向** | v2.x.x → v3.0.0 | 根本性改变 |

> **判断口诀：** 改了「怎么算对」→ 主版本；加了「还能做X」→ 次版本；只是修修补补 → 修订号。

### 迭代修改操作清单（6步）

1. **读旧版** — 确认当前版本号，读取CHANGELOG确认历史
2. **定新号** — 根据变更范围确定升哪个位
3. **写 SKILL.md** — 更新正文 + 版本号 + 日期
4. **写 CHANGELOG.md** — 在顶部插入新版详细记录（做了什么+为什么+影响三要素）
5. **同步更新 frontmatter** — 改动涉及description/name时一并更新
6. **写进化日志** — 追加到进化日志末尾
